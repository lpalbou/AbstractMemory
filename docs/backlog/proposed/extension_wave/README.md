# Extension wave backlog track (proposed)

## Status

Proposed — the nine third-pass wave items NOT yet accepted for build.
The three accepted items live in `../../planned/extension_wave/`
(0037-0039). Ledger + adversarial provenance:
`../memory_system_v1/0036_third_pass_extension_wave.md`.

## Purpose

Preserve the twice-adversarial proposal set (design concretizer +
relevance auditor, four fable5 rounds) so each item can be promoted on
the maintainer's word without re-derivation.

## Items

- `0040_recurrence_probe_and_presentation_fold.md`: duplicate-mass
  prevention. MAINTAINER: "not sure what you want to do with recurrence,
  it needs more details" — promotion requires a detail round first.
- `0041_loss_absence_anchors.md`: strongly-felt targets gone quiet →
  wake reason.
- `0042_explain_recall_serving_contract.md`: recall forensics as one
  renderable dict.
- `0043_mind_health_report.md`: formation cadence / journal growth /
  duplicate clusters / embedder pin card.
- `0044_origin_time_axis_for_imports.md`: lineage-critical — imported
  lives are temporally flat today.
- `0045_n_party_memory_contract.md`: pin N-participant formation
  conventions before multi-entity channels exist.
- `0046_transcript_adoption_verb.md`: adopt read material as memory with
  honest provenance.
- `0047_cue_hygiene_at_recall_boundary.md`: short distilled cues +
  dilution labels (mostly host lane).
- `0048_writer_forensics_for_homes.md`: append-only writer registration
  (the Hypnos class).

## Reading order

0036 ledger first; then any item independently. 0044+0046 (+ chapters
election, recorded in 0036) form the lineage bundle and should promote
together with the first real archive import.

## Governing ADRs

None identified after review.

## Scope

Proposal memory only; no implementation authorized by this track.

## Non-goals

Nothing here overrides the 0036 kills (budget guard, D2 glance,
predictions, standalone starvation read, chapter detector stay dead) or
the pending rulings Q1-Q3.

## Notes for future agents

Each item names its incident. If an incident recurs before promotion,
cite it on the item and re-present to the maintainer.
